name: Deploy Backend

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Create environment files
        run: |
          echo "${{ secrets.BACKEND_DOT_ENV }}" > .env
          echo '${{ secrets.SERVICE_ACCOUNT_JSON }}' > service-account.json

      - name: Copy code to VM
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VM_IP }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "./*"
          target: "~/AccodigiBackend_temp"
          rm: true
          exclude: "node_modules, .git, .github"

      - name: Post-deployment setup on VM
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VM_IP }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            # Explicitly source profile to find Node/NPM/PM2
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" 
            source ~/.bashrc || source ~/.profile || true
            
            # Create project directory if it doesn't exist
            mkdir -p ~/AccodigiBackend
            
            # Sync temp files to project directory
            cp -r ~/AccodigiBackend_temp/* ~/AccodigiBackend/
            
            cd ~/AccodigiBackend
            
            # Install dependencies and build
            npm install
            npx prisma generate
            npx prisma migrate deploy
            
            # Debug: verify .env existence
            ls -la ~/AccodigiBackend/.env
            
            # Restart service with PM2 (Delete and Start is more robust for env changes)
            pm2 delete truelog-backend || true
            pm2 start server.js --name truelog-backend --watch
